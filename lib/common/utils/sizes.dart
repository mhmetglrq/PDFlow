import 'package:flutter/cupertino.dart';

const paddingAll = EdgeInsets.only(left: 16, right: 16, bottom: 10);
const paddingVertical10 = EdgeInsets.symmetric(vertical: 10);

const preferredSize = Size.fromHeight(60);
